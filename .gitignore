node_modules
dist
build
.DS_Store
coverage
*.log
.vscode
.idea
.turbo

# Environment files — secrets live in Netlify dashboard.
# Patterns are anchored (/) so they only match at the repo root or the
# explicit subpath. client/.env is intentionally tracked.
/.env
/.env.local
/.env.*.local
/server/.env

# Netlify cache
.netlify/

# Claude Code session data
.claude/

# Test artefacts
test-results/
playwright-report/
